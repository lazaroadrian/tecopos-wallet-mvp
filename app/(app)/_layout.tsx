// AI-ASSISTED: generated with Claude (Gentle AI / SDD). Reviewed by <author>.
import { Stack } from 'expo-router';

/**
 * Protected app group layout — WU-1 placeholder.
 * WU-2 will add auth guard: session check + Redirect to /(auth)/login if unauthenticated.
 */
export default function AppLayout() {
  return <Stack />;
}
